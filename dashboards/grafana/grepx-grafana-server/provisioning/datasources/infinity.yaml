apiVersion: 1

datasources:
  - name: Infinity
    uid: DS_INFINITY    
    type: yesoreyeram-infinity-datasource
    access: proxy
    editable: true
    jsonData:
      baseUrl: http://localhost:5000
      